#!/usr/bin/env bash
set -euo pipefail
repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
env_file="${RAID_SIGNAL_ENV_FILE:-/home/truegrind/envs/raid-signal.env}"
docker compose --project-name raid-signal --env-file "$env_file" -f "$repo_root/deploy/compose.yaml" ps
curl --fail --silent --show-error http://127.0.0.1:3117/healthz
